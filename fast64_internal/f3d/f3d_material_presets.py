import copy
from .f3d_material_settings import *

def addMaterialPresets():
    presetDict = {}

    # Create preset, then add to dict
    
    # examplePreset = F3DMaterialSettings()
    # presetDict["Preset Name"] = examplePreset

    # WARNING: Blender saves presets based on enum index, not name.
    # Changing the order of presets will cause issues if they are in use.

    return presetDict