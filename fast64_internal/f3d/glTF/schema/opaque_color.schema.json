{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "Opaque N64 Color",
    "type": "array",
    "description": "The color is linear instead of sRGB so it must be converted for N64.",
    "items": {
        "type": "number",
        "minimum": 0.0,
        "maximum": 1.0
    },
    "minItems": 3,
    "maxItems": 3,
    "default": [
        1.0,
        1.0,
        1.0
    ]
}